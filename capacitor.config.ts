import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'assurances.f2l',
  appName: 'assurances-f2l',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
